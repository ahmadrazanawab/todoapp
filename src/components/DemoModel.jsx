import React from 'react'

const DemoModel = ({showModalBtn}) => {
  return (
    <div>
      <button className="bg-red-600 px-4 py-2 rounded-lg" onClick={showModalBtn}>Yes btn show</button>
    </div>
  )
}

export default DemoModel
